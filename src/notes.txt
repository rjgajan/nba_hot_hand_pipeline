for interactive pdq selection in arima/garch:

python -m src.interactive_fit_cli \
    path/to/ppm_series.csv \
    path/to/output_models.pkl \
    --lags 30

when done:

git add .
git commit -m "Initial pipeline setup with CLI scripts and Makefile"
git push origin main

how to run:

make all



cd ~/Projects/nba_hot_hand_pipeline

python3 -m venv venv
source venv/bin/activate

pip install --upgrade pip
pip install -r requirements.txt

make all